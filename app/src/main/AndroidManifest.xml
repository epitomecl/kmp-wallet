<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.epitomecl.kmpwallet">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".KMPWalletApp"
        android:allowBackup="true"
        android:icon="@drawable/icon_epitomecl"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_epitomecl_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>

        <activity
            android:name=".mvp.intro.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".mvp.wallet.WalletActivity" />

        <activity
            android:name=".mvp.wallet.wallets.info.InfoActivity" />

        <activity android:name=".TestActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="32d24edbaa09beeddf4288a136d4e6430d2ead23" />
    </application>


</manifest>